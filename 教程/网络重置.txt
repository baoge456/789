通过命令提示符（管理员）
搜索“cmd”或“命令提示符”。

右键点击“命令提示符”，选择“以管理员身份运行”。

输入以下命令并按回车：

text
netsh winsock reset
（重置Winsock目录，修复套接字相关错误）

输入以下命令并按回车：

text
netsh int ip reset
（重置TCP/IP协议栈）

输入以下命令并按回车：

text
ipconfig /flushdns
（清除DNS解析器缓存）